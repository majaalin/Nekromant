import React from 'react';
import Logo from './../../components/Logo';
import './home.css';

const Home = (props) => {
  return (
    <div className="Home" id="home">
      <Logo />
    </div>
  );
};

export default Home;
